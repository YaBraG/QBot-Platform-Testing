# Examples

These files are placeholders for future runnable examples.

TODO:
- Keep examples pure Python.
- Do not use ROS.
- Use `QBotMover` instead of importing backend classes directly.
- Wrap physical movement examples in `try/finally` so `qbot.stop()` always runs.

Planned examples:

| File | Purpose |
|---|---|
| `drive_forward.py` | Basic forward movement example |
| `turn_in_place.py` | Basic turn test |
| `drive_square.py` | Combined straight and turn test |
| `sensor_preview.py` | Preview selected camera or LIDAR wrapper |
